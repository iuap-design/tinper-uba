define(['text!pages/page2/page2.html'], function(template) {
	var viewModel={};
	var init=function  (argument) {
		// body...
	}

	return {
        'model': viewModel,
        'template': template,
        'init': init
    };
})